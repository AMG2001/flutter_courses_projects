import 'package:get/get.dart';
import 'package:flutter_device_type/flutter_device_type.dart';

class Dimensions {
  /**
   * TODO :
   * 3. add header with logo and actions
   */
  static double deviceWidth = Get.mediaQuery.size.width;
  static double deviceHeight = Get.mediaQuery.size.height;
 
}
